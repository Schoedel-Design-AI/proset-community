<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>
  <uses-permission android:name="android.permission.VIBRATE"/>
  <!-- Nitro Sound declares legacy storage permissions that Proset does not use. -->
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    tools:node="remove" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    tools:node="remove" />
  <!-- Location and overlay permissions from Expo/RN — not required for core functionality -->
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"
    tools:node="remove" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
    tools:node="remove" />
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"
    tools:node="remove" />
  <queries>
    <intent>
      <action android:name="android.intent.action.VIEW"/>
      <category android:name="android.intent.category.BROWSABLE"/>
      <data android:scheme="https"/>
    </intent>
  </queries>
  <application android:name=".MainApplication" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:allowBackup="false" android:theme="@style/AppTheme" android:supportsRtl="true">
    <service
        android:name="ms.aifor.app.recording.RecordingForegroundService"
        android:enabled="true"
        android:exported="false"
        android:foregroundServiceType="microphone" />
    <activity android:name=".MainActivity" android:screenOrientation="sensorPortrait" android:configChanges="keyboard|keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout|uiMode" android:launchMode="singleTop" android:windowSoftInputMode="adjustResize" android:theme="@style/AppTheme" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="proset"/>
        <data android:scheme="ms.aifor.app"/>
      </intent-filter>
      <!-- Android App Links. Lets https://proset.ai app routes open Proset
           instead of the browser, so a completed web checkout (or an email
           action link) hands the customer back into the app holding their
           entitlement. Verified against
           https://proset.ai/.well-known/assetlinks.json, which is served by the
           Cloud Run app and lists both the Play App Signing certificate and the
           upload key. If verification fails, Android silently falls back to the
           browser - the pre-existing behaviour, so this degrades safely.

           Paths are scoped to the app's own routes on purpose: the landing page,
           /documentation, /privacy, /refund and /terms should stay on the web. -->
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="https"/>
        <data android:host="proset.ai"/>
        <data android:host="www.proset.ai"/>
        <data android:path="/choose-plan"/>
        <data android:path="/login"/>
        <data android:path="/welcome"/>
        <data android:path="/record"/>
        <data android:path="/recordings"/>
        <data android:path="/combine"/>
        <data android:path="/files"/>
        <data android:path="/music"/>
        <data android:path="/settings"/>
        <data android:path="/admin"/>
        <data android:path="/mfa-setup"/>
        <data android:path="/force-change-password"/>
        <data android:path="/verify-email"/>
        <data android:path="/reset-password"/>
        <data android:path="/thought-threads"/>
        <data android:pathPrefix="/recording/"/>
      </intent-filter>
    </activity>
    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/filepaths" />
    </provider>
  </application>
</manifest>
