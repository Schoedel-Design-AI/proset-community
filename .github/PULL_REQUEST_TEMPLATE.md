## Description

<!-- What does this PR do? Link the issue it closes, e.g. "Fixes #123" -->

## Checklist

- [ ] `npx tsc --noEmit` passes
- [ ] Tests run: `node --import tsx --test tests/` (or the relevant subset)
- [ ] `CHANGELOG.md` entry added if user-facing
- [ ] Commits carry `Signed-off-by:` (DCO) — `git commit -s`
- [ ] No secrets, no billing code, no internal/super-admin surfaces
- [ ] `.env.example` updated if environment variables changed

## Notes for reviewers

<!-- Scope, tradeoffs, testing done, anything else the reviewer should know -->
